export interface registerData {
    firstName: string;
    lastName: string;
    birthDate: string;
    country: string;
    city: string;
    email: string;
    password: string;
    confirmPassword: string;
}

export interface loginData {
    email: string;
    password: string;
}